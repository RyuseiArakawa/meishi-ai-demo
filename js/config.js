/* 版の番号。index.html と照らし合わせて、古いファイルが残っていないか確かめます。 */
(window.APP_BUILD = window.APP_BUILD || {})["config"] = 14;

/* =============================================================================
   設定ファイル

   ★このファイルだけ、あなたが書き換えます。★

   Cloudflare Workers を作ると、
     https://なにか.あなたの名前.workers.dev
   というURLがもらえます。それを下の "" の中に貼り付けてください。

   末尾のスラッシュ ( / ) は付けません。

   例：
     API_ENDPOINT: "https://meishi-ocr.taro-yamada.workers.dev",

   ここにAPIキーを書いてはいけません。書く必要もありません。
   ============================================================================= */

window.APP_CONFIG = {

  API_ENDPOINT: "https://meishi-ocr.muetai250115.workers.dev",

  /* 1分あたりに送ってよい回数。
     Gemini の無料枠は、モデルにもよりますが 1分あたり 10〜15 回です。
     これを超えると「レート制限」になり、しばらく使えなくなります。
     名刺をまとめて読み取るときは、この値に合わせて間隔を空けます。

     Google Cloud で請求先を設定すると上限が大きく上がるので、
     その場合は 60 などに増やしてください（速く処理できます）。 */
  REQUESTS_PER_MINUTE: 10,

};