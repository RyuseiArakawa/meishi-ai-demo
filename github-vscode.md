# 手順4（VS Code版）― GitHub に上げて公開する

README の「4. GitHub に上げて公開する」を、この手順に置き換えてください。
手順1〜3（APIキーの取得、Cloudflare Workers の作成）は、そのまま先に済ませておきます。

VS Code から公開すると、あとで直したいときに
**「直す → 送る」がボタン2つで済む**ようになります。
ブラウザでファイルを上げ直す必要がなくなるので、最初に少し手間をかける価値があります。

---

## 用語の説明

作業に入る前に、3つの言葉だけ覚えてください。

| 言葉 | 意味 | 身近な例え |
|---|---|---|
| **Git**（ギット） | ファイルの変更を記録する仕組み | 書類の「変更履歴」機能 |
| **コミット** | 「ここまでの変更を記録する」操作 | 上書き保存＋メモを残す |
| **プッシュ／同期** | 記録をGitHubへ送る操作 | 保存した書類をサーバーに送る |

VS Code の左側にある「枝分かれしたマーク」が、この作業をする場所です。

---

## 4-1. Git がパソコンに入っているか確認する

VS Code とは別に、**Git** という道具が必要です。VS Code はこれを内部で使っています。

1. VS Code を開く
2. 上のメニューから **「ターミナル」→「新しいターミナル」** を選ぶ
   （画面の下半分に、文字を打ち込む黒い領域が出ます）
3. 次の文字を打って、Enter キーを押す

   ```
   git --version
   ```

**`git version 2.xx.x` のように表示されたら → 入っています。4-2 へ進んでください。**

### 入っていなかった場合

| パソコン | やること |
|---|---|
| **Windows** | https://git-scm.com/download/win を開いてダウンロード。インストーラーは、選択肢を変えずに「Next」を押し続けて構いません |
| **Mac** | ターミナルに `git --version` と打つと「インストールしますか」と聞かれるので、そのまま承諾します |

インストール後、**VS Code をいったん閉じて開き直してから**、もう一度 `git --version` を確認してください。

---

## 4-2. 名前とメールアドレスを登録する

Gitは「誰が変更したか」を記録するため、最初に一度だけ名前の登録が必要です。
これを飛ばすと、あとの手順でエラーになります。

ターミナルに、次の2行を **1行ずつ** 打ち込んで Enter を押します。

```
git config --global user.name "あなたの名前"
```

```
git config --global user.email "あなたのメールアドレス"
```

- 名前は本名でなくて構いません（GitHubのユーザー名でもよい）
- メールアドレスは、GitHubに登録したものを使ってください
- **ダブルクォーテーション `"` は必要です**。消さないでください

何も表示されずに次の行が出れば、成功しています。

---

## 4-3. VS Code から GitHub にログインする

1. VS Code の**左端の縦に並んだアイコン**の、一番下にある**人型のマーク**（アカウント）を押す
2. **「Sign in with GitHub」** または **「GitHubでサインイン」** を選ぶ
3. ブラウザが開くので、GitHub にログインして **「Authorize」**（許可）を押す
4. 「Visual Studio Code で開きますか」と聞かれたら **「開く」**

VS Code に戻り、人型マークのところにあなたのGitHubユーザー名が表示されていれば成功です。

---

## 4-4. フォルダを開く

**ここが一番間違えやすい場所です。**

VS Code のメニューから **「ファイル」→「フォルダーを開く」** を選び、
**`meishi-ai-demo` フォルダそのもの** を選んでください。

```
✅ 正しい                     ❌ まちがい
                              
meishi-ai-demo   ← これを選ぶ  ダウンロード   ← これを選んでしまう
├── index.html                 └── meishi-ai-demo
├── css/                            ├── index.html
└── js/                             └── ...
```

**選び方の目安：フォルダを開いたあと、VS Code の左側の一覧に `index.html` が
すぐ見えていれば正しい状態です。**
`meishi-ai-demo` というフォルダ名を一度クリックしないと `index.html` が出てこない場合は、
1つ上の階層を開いてしまっています。開き直してください。

（間違えると、公開後のURLが `.../meishi-ai-demo/meishi-ai-demo/` と二重になり、
ページが表示されません）

「このフォルダー内のファイルの作成者を信頼しますか？」と聞かれたら、
**「はい、作成者を信頼します」** を押してください。

---

## 4-5. 変更を記録する（コミット）

1. 左端のアイコンから、**枝分かれしたマーク**（ソース管理）を押す
2. **「リポジトリを初期化する」**（Initialize Repository）というボタンを押す
   → ファイルの一覧が表示されます
3. 上の入力欄に、今回の作業内容を書きます。日本語で構いません

   ```
   Phase 1 名刺登録画面
   ```

4. **「コミット」**（Commit）ボタンを押す
5. 「ステージングされていない変更をすべてコミットしますか？」と聞かれたら
   **「はい」** または **「Yes」** を押す

これで、パソコンの中に記録が作られました。まだGitHubには届いていません。

---

## 4-6. GitHub に送る（公開）

1. 同じ画面にある **「Branch の発行」**（Publish Branch）ボタンを押す
2. 画面の上に選択肢が2つ出ます。**必ず下を選んでください**

   ```
   Publish to GitHub private repository   ← ❌ 選ばない
   Publish to GitHub public repository    ← ✅ こちらを選ぶ
   ```

   > **private（非公開）を選ぶとGitHub Pagesが使えません。**
   > 無料プランでは、公開リポジトリでないとWebページとして表示できないためです。

3. リポジトリ名を聞かれたら `meishi-ai-demo` と入力して Enter
4. 「Successfully published」と表示されれば完了です

ブラウザで `https://github.com/あなたの名前/meishi-ai-demo` を開き、
ファイルが並んでいることを確認してください。

---

## 4-7. GitHub Pages をオンにする

**この作業だけはブラウザで行います**（VS Code からは設定できません）。一度きりの作業です。

1. `https://github.com/あなたの名前/meishi-ai-demo` を開く
2. 上のタブから **「Settings」** を押す
3. 左メニューの **「Pages」** を押す
4. **Source** を **「Deploy from a branch」** にする
5. **Branch** を **`main`**、右側のフォルダを **`/ (root)`** にして **「Save」**
6. 1〜2分待ってページを再読み込みすると、上部にURLが出ます

   ```
   https://あなたの名前.github.io/meishi-ai-demo/
   ```

---

## 4-8. これ以降の直し方（ここからが本題です）

一度この形にしておくと、修正のたびにファイルを上げ直す必要がなくなります。

たとえば **手順5** の `js/config.js` にURLを書き込むときは、こうします。

1. VS Code の左の一覧から `js/config.js` をクリックして開く
2. `API_ENDPOINT: "",` の `""` の中に、WorkerのURLを貼り付ける
3. **`Ctrl + S`**（Macは `Command + S`）で保存
4. 枝分かれマーク（ソース管理）を押す
5. 入力欄に `Worker のURLを設定` と書く
6. **「コミット」** を押す
7. **「変更の同期」**（Sync Changes）を押す

1分ほどで、公開ページに反映されます。

**覚えるのはこの3つだけです。**

```
保存（Ctrl+S） → コミット → 同期
```

Phase 2以降でファイルを追加していくときも、この繰り返しになります。

---

## うまくいかないとき

| 表示 | 原因 | 直し方 |
|---|---|---|
| `git: command not found` | Gitが未インストール | 4-1 をやり直し、VS Codeを再起動 |
| `Please tell me who you are` | 名前の登録漏れ | 4-2 の2行を実行する |
| 「リポジトリを初期化する」が出ない | すでに初期化済み | そのまま 4-5 の手順3へ進む |
| 「Branch の発行」が見当たらない | GitHub未ログイン | 4-3 をやり直す |
| Pages の画面に `main` が出てこない | まだ送信できていない | 4-6 をやり直す |
| ページを開くと404 | フォルダの選び間違い、または反映待ち | 4-4 を確認。正しければ2分待つ |
| ページが真っ白 | `index.html` がリポジトリの直下にない | 4-4 のとおり開き直す |

---

## 秘密情報について

このプロジェクトには `.gitignore` というファイルを入れてあります。
`.env` やAPIキーを含むファイルを、うっかりGitHubに送ってしまうことを防ぐものです。

`worker/worker.js` にはAPIキーは書かれていません（キーはCloudflare側に預けてあります）ので、
GitHubに上げても問題ありません。
